# -*- coding: utf-8 -*-

import time
import random

def External():
    while true
        time.wait(300)
        factor = random.randint(1,100)
        if factor > 98 :
            if factor == 99 :
                SendDiplomaticTensionSignal() #Communication through signals
            else:
                SendInondationSignal() #Communication through signals
